// Simple Example
const language = "JavaScript";
const inventor = "Brendan Eich";
const invented = 1995;

console.log(`${language} was created by ${inventor} in ${invented}.`);

// Calculation Example
const price = 100;
const quantity = 2;
const total = price * quantity;
console.log(`The total cost of ${quantity} ${language} books is $${total}.`);
